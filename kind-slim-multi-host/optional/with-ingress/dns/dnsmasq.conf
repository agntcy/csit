# Generated by render-dnsmasq.sh
# This file is a CoreDNS Corefile consumed by optional/with-ingress/dns/docker-compose.yaml.
.:53 {
    errors
    # Keep query logs visible in container stdout/stderr.
    log

    # Explicit records used by optional ingress manifests.
    hosts {
        127.0.0.1 control.cluster-a.csit.test
        127.0.0.1 slim.cluster-a.csit.test
        127.0.0.1 slim.cluster-b.csit.test
        fallthrough
    }

    # Catch-all for *.csit.test so additional ingress hostnames resolve to loopback.
    template IN A {
        match (.*\.)?csit\.test\.
        answer "{{ .Name }} 60 IN A 127.0.0.1"
    }

    # Resolve everything else through upstream DNS.
    forward . 1.1.1.1
}
