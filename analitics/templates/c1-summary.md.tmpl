# C1 Evidence Summary

**Generated at:** {{GENERATED_AT_UTC}}  
**Scope:** C1 use cases only (`request-reply`, `fire-and-forget`, `write`)  
**Source contract:** `docs/plans/slim-c1-evidence-contract-v1.md`

## Goal

Summarize C1 evidence for SLIM from the local smoke benchmark report set, aligned
to the agentic systems taxonomy and the C1 use-case contract.

## Taxonomy context (condensed)

- **C1 Centralized:** one authority controls routing/invocation.
- **C2 Decentralized:** workflow graph coordinates communication.
- **C3 Distributed:** federated peers across hosts/clusters.

This summary intentionally covers only **C1**.

## C1 use-case status

| Row ID | Use case | Mechanism | Status | Runs seen | Evidence |
|--------|----------|-----------|--------|-----------|----------|
| `c1-request-reply` | Agent waits for reply | `request-reply` | {{STATUS_REQUEST_REPLY}} | {{ROWS_REQUEST_REPLY}} | `published/smoke/c1-evidence.json` |
| `c1-fire-and-forget` | Agent emits async event | `fire-and-forget` | {{STATUS_FIRE_AND_FORGET}} | {{ROWS_FIRE_AND_FORGET}} | `published/smoke/c1-evidence.json` |
| `c1-write` | Publish without paired responder | `write` | {{STATUS_WRITE}} | {{ROWS_WRITE}} | `published/smoke/c1-evidence.json` |

## Evidence sources

- `analitics/reports/c1-evidence.json` (primary — Ginkgo assertion report)
- `analitics/tests/c1_evidence_test.go` (C1 scenario definitions)

## Re-run command

```bash
task -t analitics/Taskfile.yml dashboard:build
```

Or run only the evidence tests:

```bash
task -t analitics/Taskfile.yml test:c1-evidence
```
